'use strict';

/**
 * golf-sponsor-category router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::golf-sponsor-category.golf-sponsor-category');
