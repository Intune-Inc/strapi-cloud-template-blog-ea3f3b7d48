'use strict';

/**
 * golf-sponsor-support service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::golf-sponsor-support.golf-sponsor-support');
