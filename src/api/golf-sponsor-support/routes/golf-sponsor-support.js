'use strict';

/**
 * golf-sponsor-support router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::golf-sponsor-support.golf-sponsor-support');
